# ServiceMonitor for DX03 Backend
# Configures Prometheus to scrape metrics from backend API

---
apiVersion: v1
kind: Service
metadata:
  name: dx03-backend-metrics
  namespace: dx03-dev
  labels:
    app: dx03-backend
    component: metrics
spec:
  selector:
    app: dx03-backend
  ports:
  - name: metrics
    port: 3000
    targetPort: 3000
    protocol: TCP
  type: ClusterIP

---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: dx03-backend
  namespace: monitoring
  labels:
    app: dx03-backend
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: dx03-backend
      component: metrics
  namespaceSelector:
    matchNames:
    - dx03-dev
  endpoints:
  - port: metrics
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s

name: "🔧 Apply Istio Configurations"

on:
  workflow_dispatch:
    inputs:
      restart_pods:
        description: 'Restart pods to inject sidecars'
        required: true
        default: 'true'
        type: boolean
      apply_configs:
        description: 'Apply Istio configurations (gateway, telemetry, security)'
        required: true
        default: 'true'
        type: boolean
      force_delete:
        description: 'Force delete and recreate pods (instead of restart)'
        required: true
        default: 'false'
        type: boolean

env:
  GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  GCP_REGION: us-central1
  CLUSTER_NAME: tx03-gke-cluster

jobs:
  apply-istio-configs:
    name: "Configure Istio Service Mesh"
    runs-on: ubuntu-latest
    
    permissions:
      contents: read
      id-token: write
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔐 Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}

      - name: ☁️ Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.GCP_PROJECT_ID }}
          install_components: 'gke-gcloud-auth-plugin'

      - name: 🔧 Configure kubectl
        run: |
          gcloud container clusters get-credentials ${{ env.CLUSTER_NAME }} \
            --region ${{ env.GCP_REGION }} \
            --project ${{ env.GCP_PROJECT_ID }}

      - name: �️ Force Delete Pods (if requested)
        if: inputs.force_delete == true
        run: |
          echo "🗑️ Deleting all pods to force sidecar injection..."
          
          kubectl delete pod --all -n dx03-dev
          
          echo "⏳ Waiting for new pods to be created (60s)..."
          sleep 60
          
          echo "📊 New pods status:"
          kubectl get pods -n dx03-dev
          
          echo ""
          echo "✅ Pods deleted and recreated"

      - name: 🔄 Restart Pods for Sidecar Injection
        if: inputs.restart_pods == true && inputs.force_delete == false
        run: |
          echo "🔄 Restarting dx03 pods to inject Istio sidecars..."
          
          kubectl rollout restart deployment/dx03-backend -n dx03-dev
          kubectl rollout restart deployment/dx03-frontend -n dx03-dev
          
          echo "⏳ Pods are being restarted..."
          echo "Note: On GKE Autopilot, sidecar injection may take 10-15 minutes due to resource provisioning."
          echo "Check rollout status manually with:"
          echo "  kubectl rollout status deployment/dx03-backend -n dx03-dev"
          echo "  kubectl rollout status deployment/dx03-frontend -n dx03-dev"
          
          echo ""
          echo "✅ Rollout initiated successfully"

      - name: 🌐 Apply Istio Configurations
        if: inputs.apply_configs == true
        run: |
          echo "🌐 Applying Istio configurations..."
          
          if [ -f "k8s/istio/gateway.yaml" ]; then
            kubectl apply -f k8s/istio/gateway.yaml
            echo "✅ Gateway configuration applied"
          fi
          
          if [ -f "k8s/istio/telemetry.yaml" ]; then
            kubectl apply -f k8s/istio/telemetry.yaml
            echo "✅ Telemetry configuration applied"
          fi
          
          if [ -f "k8s/istio/security.yaml" ]; then
            kubectl apply -f k8s/istio/security.yaml
            echo "✅ Security policies applied (PERMISSIVE mTLS)"
          fi
          
          echo ""
          echo "✅ All Istio configurations applied!"

      - name: 🔍 Diagnose Sidecar Injection
        if: inputs.restart_pods == true || inputs.force_delete == true
        run: |
          echo "======================================================================"
          echo "🔍 DIAGNÓSTICO DE SIDECAR INJECTION"
          echo "======================================================================"
          echo ""
          
          echo "1️⃣ Namespace Label:"
          kubectl get namespace dx03-dev --show-labels | grep istio-injection
          echo ""
          
          echo "2️⃣ Mutating Webhook:"
          kubectl get mutatingwebhookconfiguration istio-sidecar-injector -o yaml | grep -A10 namespaceSelector || echo "⚠️ Webhook not found"
          echo ""
          
          echo "3️⃣ Pods with Container Count:"
          kubectl get pods -n dx03-dev -o jsonpath='{range .items[*]}{.metadata.name}{"\t"}{.spec.containers[*].name}{"\n"}{end}'
          echo ""
          
          echo "4️⃣ Checking Sidecar Injection:"
          TOTAL_PODS=$(kubectl get pods -n dx03-dev -o json | jq '.items | length')
          PODS_WITH_SIDECAR=$(kubectl get pods -n dx03-dev -o json | jq '[.items[] | select(.spec.containers | length > 1)] | length')
          
          echo "Total pods: $TOTAL_PODS"
          echo "Pods with sidecar: $PODS_WITH_SIDECAR"
          
          if [ "$TOTAL_PODS" -eq "$PODS_WITH_SIDECAR" ] && [ "$TOTAL_PODS" -gt 0 ]; then
            echo "✅ SUCCESS! All pods have sidecars injected!"
          else
            echo "❌ WARNING! Some pods may not have sidecars"
            echo "This is expected if pods are still being created."
          fi
          echo ""

      - name: ✅ Validate Installation
        run: |
          echo "======================================================================"
          echo "🎯 Istio Service Mesh Validation"
          echo "======================================================================"
          echo ""
          
          echo "=== Deployments Status ==="
          kubectl get deployments -n dx03-dev
          echo ""
          
          echo "=== ReplicaSets Status ==="
          kubectl get replicasets -n dx03-dev
          echo ""
          
          echo "=== Recent Events (last 20) ==="
          kubectl get events -n dx03-dev --sort-by='.lastTimestamp' | tail -20
          echo ""
          
          echo "=== Istio System Pods ==="
          kubectl get pods -n istio-system
          echo ""
          
          echo "=== Application Pods (should show 2/2 containers after sidecar injection) ==="
          kubectl get pods -n dx03-dev
          echo ""
          
          echo "=== Istio Gateway ==="
          kubectl get gateway -n dx03-dev
          echo ""
          
          echo "=== Istio VirtualService ==="
          kubectl get virtualservice -n dx03-dev
          echo ""
          
          echo "=== Istio DestinationRules ==="
          kubectl get destinationrule -n dx03-dev
          echo ""
          
          echo "======================================================================"
          echo "✅ Istio Configuration Complete!"
          echo "======================================================================"
          echo ""
          echo "📚 Access Kiali dashboard:"
          echo "   kubectl port-forward -n istio-system svc/kiali 20001:20001"
          echo "   http://localhost:20001"
          echo ""
          echo "📊 Access Jaeger tracing:"
          echo "   kubectl port-forward -n istio-system svc/jaeger 16686:16686"
          echo "   http://localhost:16686"

      - name: 📤 Send Slack notification on success
        if: success()
        uses: slackapi/slack-github-action@v1.24.0
        continue-on-error: true
        with:
          payload: |
            {
              "text": "✅ Istio Configurations Applied Successfully",
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "✅ *Istio Service Mesh Configured*\n\n*Restart Pods:* ${{ inputs.restart_pods }}\n*Apply Configs:* ${{ inputs.apply_configs }}\n\n*Next:* Access Kiali dashboard to view service mesh\n`kubectl port-forward -n istio-system svc/kiali 20001:20001`"
                  }
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

      - name: 📤 Send Slack notification on failure
        if: failure()
        uses: slackapi/slack-github-action@v1.24.0
        continue-on-error: true
        with:
          payload: |
            {
              "text": "❌ Istio Configuration Failed",
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "❌ *Istio Configuration Failed*\n\nCheck workflow logs for details."
                  }
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
